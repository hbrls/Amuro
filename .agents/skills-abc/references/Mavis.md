# 任务：abc定时任务骨架

> 本文件是定时任务模板，当前阶段用于驱动 cron链路验证。3/4/5 节在验证阶段不启用。

## 0. 项目根目录

项目根目录固定为 `D:/OverseaFront/Glintz/`，写死不再询问。后续步骤中的 `D:/OverseaFront/Glintz` 均指此路径。

## 任务执行原则

- SKILL.md处于骨架阶段是当前真实状态，不是不完整——照执行。
-任务模板中未启用的节（如当前未启用的3/4/5 节）是模板结构本身，不代表任务残缺。
-步骤里给出的命令就是规范，照执行；不要先验证存在性再决定是否执行。

## 1.读 SKILL 并执行

读取并按 SKILL.md 方法论执行：

```bash
cat D:/OverseaFront/Glintz/.agents/skills/abc/SKILL.md
```

按 SKILL.md 方法论执行——重点关注：

- 「方案决策表」 / 「内容处理规则」 / 「目标结构」 / 「命名约定」 ——通用方法论（abc 当前阶段未定义）
- 「本项目结构约束」 —— **本项目特有的约束 /角色白名单 /同步点**都在这一节查，本文件不重复

本次是定时任务，无人确认。子目录划分、命名、入口形态按 SKILL.md规则自主判断。
